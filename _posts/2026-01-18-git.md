---
title: "Git CheatSheet"
date: 2026-01-18 00:00:00 +0800
categories: [Web Exploitation]
tags: [git, gitcheatsheet]
platform: Web
author: 0xmr
image: /assets/img/posts/git.webp
excerpt: "cheat sheet for github commands"
---

# GitHub
- Git is the tool that tracks changes to files over time (like version history).
- GitHub is the service that hosts Git repositories online and provides a user-friendly interface for collaboration.


# Resources
[github-cheatsheet](https://education.github.com/git-cheat-sheet-education.pdf)

## Git Clone Repo
```bash
# with ssl verifying
git clone url

# without ssl verifying
git -c http.sslVerify=false clone $url
GIT_SSL_NO_VERIFY=true git clone  $url
```
## Download .git files
### Git-Dumper
Usage:-
```bash
git-dumper http://google.com/.git/  /path/to/save

git-dumper http://google.com/.git/  .
```
### Git-Hack
Usage:-
```bash
githack http://google.com/ path/to/save 

githack http://google.com/ .
```
## Analyzing .git leaks
> trufflehog
Usage:-

```bash
trufflehog file://$(pwd)   #It scan the current Directory for Git leaks....
```
> gitleaks
Usage:-

```bash
gitleaks detect  --source .
gitleaks detect --no-git --source /path/to/scan
```

> Manually Way's
Usage:-

```bash
cat .git/logs/HEAD   # Grab the Second Raw Hashes

# Find Secrets...
git show <Paste Hash Here>
```


# cheatsheet
### Check Commits
```bash
git log | grep -i 'commit'
or
git log --oneline

git show <Commit_Hash>
git log --oneline --graph --all              <-- with branch

# Automation
git log --oneline | grep -iE [a-zA-Z0-9] | cut -d ' ' -f1 > commits
for i in $(cat commits); do git show ${i};done    <--Press Enter Enter
```

### Check author's
```bash
# Simple
git log | grep -i 'author' 
git log | grep -i 'author' | cut -d ':' -f2

# Automation
git log -p --all | grep -iE "postgres|DATABASE_URL|sqlalchemy|SQL|passwd|api_key|secret|db|db_name|username"
git log -p --all | grep -iE "postgres|DATABASE_URL|sqlalchemy|SQL|passwd|api_key|secret|db|db_name|username|secret_key|aws|aws_key|http"
git log -p --all --full-history | grep -iE "(postgres|mysql|mongodb|DATABASE_URL|DB_URL|sqlalchemy|sqlite|redis|connection_string|passwd|password|pwd|api_key|api_secret|secret_key|secret|token|auth_token|bearer|private_key|access_key|aws_key|aws_access|aws_secret|AWS_ACCESS_KEY|AWS_SECRET|S3_BUCKET|http|https|ftp|endpoint|base_url|host|port|username|user|db|db_name|MONGO_URI|REDIS_URL|JWT_SECRET|NEXTAUTH|STRIPE|SENDGRID|TWILIO|firebase|supabase|clerk|oauth|client_id|client_secret)" 
```

### Check Deleted files
```bash
git log --diff-filter=D --summary | grep delete
```
### Restore Deleted files
```bash
git restore <File_Name>
```
### Check HEAD logs
```bash
cat /.git/logs/HEAD 

# show Senstive Data
git show <Commit_Hashes>
```
### check status
```bash
# first check the  git status
git status 

# If the files modifyed or changes are happen , run this 
git diff --cached <modified file name>
```
### Check Git logs
```bash
git log
```
### check difference in file
```bash
git diff
```
